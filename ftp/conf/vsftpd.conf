listen=YES
listen_ipv6=NO
anonymous_enable=NO
pam_service_name=vsftpd.mysql
guest_enable=YES
guest_username=${GUEST_USER}
virtual_use_local_privs=YES
chroot_local_user=YES
allow_writeable_chroot=YES
local_enable=YES
write_enable=YES
user_config_dir=${FTP_USERS_DIR}
pasv_enable=YES
pasv_min_port=${PASV_MIN}
pasv_max_port=${PASV_MAX}
hide_ids=YES
seccomp_sandbox=YES
use_localtime=YES
xferlog_enable=YES
log_ftp_protocol=YES
vsftpd_log_file=/var/log/vsftpd.log
dual_log_enable=YES
local_umask=022
connect_from_port_20=YES
secure_chroot_dir=/var/run/vsftpd
ftp_username=${GUEST_USER}
chown_username=${GUEST_USER}
user_sub_token=$USER
check_shell=NO

